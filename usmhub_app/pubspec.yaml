name: usmhub_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  flutter_rating_bar: ^4.0.1

  # Firebase Messaging para notificaciones push
  firebase_messaging: ^15.1.3
  # URL Launcher para redireccionar a sitios externos
  url_launcher: ^6.3.0
  # Calendar para integrar el calendario
  table_calendar: ^3.1.2
  # Searchable Dropdown para implementar búsqueda en tiempo real
  dropdown_search: ^6.0.1
  # Firebase Core para inicializar Firebase
  firebase_core: ^3.6.0
  # Firebase Cloud Firestore para almacenar datos de los sitios y calificaciones (opcional)
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.jpg"

flutter:
  uses-material-design: true

